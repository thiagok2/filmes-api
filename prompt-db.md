Vamos agora adicionar a camada de persistencia com uso do mysql e sequelize. Vamos fazendo o passo a passo:

1. com install das libs: mysql, sequelize, dotenv 
2. configuracao do .env com senha, anteromente instalar o dotenv.

3. quero fazer arquivos de repositorio individuais para filmes, favoritos, e comentarios, como as operações tradicionais de CRUD(insert, select, update, delete)